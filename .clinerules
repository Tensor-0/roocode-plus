# RooCode Plus — 代码规范

## 模型调用工具使用规则

### 写文件操作
- **强制使用 `write_to_file`**：所有文件创建和完整重写操作一律使用 `write_to_file` 工具。
- **禁用 `apply_diff`**：本项目禁止使用 `apply_diff` 进行增量修改，避免因上下文截断导致 diff 匹配失败。
- **写入前检查 `path` 参数**：确保目标路径在项目目录范围内，防止意外覆盖系统文件。

### 原因说明
部分大模型在处理大型 diff 块时，可能因 token 上下文限制产生不完整的 SEARCH/REPLACE 块，导致 `apply_diff` 失败。`write_to_file` 完整写入模式更为可靠。

### 执行流程
1. 使用 `read_file` 完整读取目标文件
2. 在内存中完成全部修改
3. 使用 `write_to_file` 一次性写回完整内容
4. 验证写入成功后再继续下一步
